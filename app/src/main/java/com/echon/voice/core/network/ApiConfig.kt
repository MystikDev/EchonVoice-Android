package com.echon.voice.core.network

/** Central host config for the echon-voice.com /v1 API. */
object ApiConfig {
    const val BASE_URL = "https://echon-voice.com/"
    const val WS_URL = "wss://echon-voice.com/v1/ws"
}
